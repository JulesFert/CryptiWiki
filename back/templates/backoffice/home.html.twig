{% extends '@EasyAdmin/layout.html.twig' %}


{% block title %}Back-Office{% endblock %}

{% block content %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/easyadmin-custom.css') }}">
{% endblock %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> #}

<div class="">

<h1 class="home__title">Bienvenue sur le Back-Office de CryptiWiki !</h1>

<h3>Articles en attentes de validations :</h3>

<div class="card">
<ul class="article__list">

    {% for creature in creatures %}

        {% if creature.isValid is same as false %}

        {% if creature.updatedAt is null %}

            <li class="article__item">{{ creature.name }} - Créée le {{ creature.createdAt|date("d/m/y") }}</li>

        {% else %}
            <li class="article__item">{{ creature.name }} - Modifié le {{ creature.updatedAt|date("d/m/y") }}</li>
        {% endif %}

        {% endif %}
    {% endfor %}

</ul>
</div>
</div>
{% endblock %}
